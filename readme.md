# CSC - hw1
2025_Spring

// everytime for attacker vm's terminal:
```sudo sysctl -w net.ipv4.ip_forward=1```

    in vm, the RSA key is stored in folder path: '/home/ubuntu/.ssh/'
    and to generate rsa-key, type ```ssh-keygen```
    copy the public key to github server!! 
    git clone by ssh is now available!